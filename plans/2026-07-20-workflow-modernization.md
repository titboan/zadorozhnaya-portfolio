# План: модернизация workflow (Figma-лимиты, ручная работа, инструменты)

Статус: в работе

## Контекст

Отправная точка — Figma MCP на бесплатном тарифе ограничен **6 вызовами чтения в месяц** (не вопрос аккуратности запросов, это жёсткая квота), плюс REST API так же порезан (см. `retrospectives/2026-06-18_figma-export-rate-limit.md`). Заодно разобрали, какие ещё бесплатные инструменты закрывают повторяющуюся ручную работу (тяжёлые картинки из Figma, отсутствие проверки деплоя).

Принцип: Боря — дизайн, Клод — код. Всё, что можно снять с Бори автоматизацией на моей стороне, снимаем; всё, что требует его аккаунта/выбора — явно помечено ниже.

---

## Часть 1 — Автоматизация картинок (без внешних зависимостей, можно сразу)

**Кто делает:** Клод

- [x] Поставить `vite-plugin-image-optimizer` (Sharp + SVGO, сжатие при сборке)
- [x] Настроить сжатие JPEG/PNG/SVG для `public/images` (`includePublic: true` по умолчанию)
- [x] Прогнать билд, убедиться что вес страниц с кейсами (АГРОЦЕНТР, Провыходной) заметно упал — итог: `dist` 202 МБ → 22 МБ (экономия ~85%)
- [x] Закрывает боль из ретро: `work-3-poly.jpg` весил 16 МБ, приходилось вручную гонять `download_assets` с `defaultScale` под каждую картинку

Побочный найденный баг (не часть плана, исправлено попутно): `public/images/projects/other/portfolio.jpg` — 59 МБ, 32768×23929px, нигде не использовался в коде и не проходил сжатие (sharp упирался в лимит пикселей) — удалён как мёртвый файл.

Конвертация в WebP/AVIF не сделана — риск сломать пути (везде в `projects.js`/`case-studies.js` захардкожены `.jpg`), нужен отдельный проход с `<picture>`-разметкой. Оставлено на будущую сессию, если понадобится.

**Найдено при проверке (не в этом плане), починено 2026-07-20:** в `src/data/projects.js` два пути к картинкам были битые — имя файла на диске склеено с текстом описания задачи (`flebologiya` и `personal-brand`). Пересекалось с пунктом «Уточнить тексты карточек» в `plans/2026-06-28-project-pages.md` — там и починено (коммит `8226ed7`).

## Часть 2 — Доступность (без внешних зависимостей)

**Кто делает:** Клод

- [x] Поставить `eslint-plugin-jsx-a11y` — вместе с базовым ESLint 9 (в проекте его раньше не было вообще), `eslint-plugin-react`, `eslint-plugin-react-hooks`. Flat config в `eslint.config.js`, команда `npm run lint`
- [x] Прогнать, поправить находки — 0 ошибок на всех 16 файлах `src/`. Проверено контрольным примером (`<img>` без `alt`), что правило реально ловит нарушения — не ложноположительная тишина

Грабля при настройке: в ESLint 9 flat config `files: ['**/*.{js,jsx}']` нужно указывать явно в каждом блоке — без него `eslint .` тихо пропускает все `.jsx`-файлы и репортит 0 без единой ошибки, выглядит как «всё чисто», хотя на деле ничего не проверялось. Зафиксировано в `ai-clone/feedback/`.

## Часть 3 — Vercel MCP (проверка деплоя)

**Кто делает:** Боря подключает, Клод использует

- [x] Боря включает коннектор Vercel в настройках коннекторов claude.ai (бесплатно, официальный коннектор Anthropic) — подключён, `claude mcp list` показывает `claude.ai Vercel — ✓ Connected` (2026-07-20)
- [ ] Дальше Клод после каждого пуша в `master` сам проверяет статус сборки/деплоя вместо «наверное сработало» — коннектор подключён, но инструменты Vercel не появились в текущей сессии (нужен рестарт сессии, чтобы Claude Code подхватил новый MCP-сервер) — доделать в следующей сессии
- [ ] Закрывает боль: `package.json` с Lenis не закоммитили → Vercel build упал, узнали не сразу

Временная замена без коннектора (сделано 2026-07-20): после пуша коммита с оптимизацией картинок проверил вручную через `curl` по продовому URL — главная страница и `/case/logo` отдают 200, JS-бандл грузится, `work-1-logo.jpg` весит 458.69 КБ (ровно как в сжатой сборке, было 1880 КБ до оптимизации). Значит деплой прошёл и подхватил именно новую версию. Это не замена коннектора — просто ручная проверка «после пуша», работает только для внешних URL (не для логов сборки Vercel и не для localhost — см. ограничение ниже).

## Часть 4 — Figma → Lunacy, локально через VS Code (снимает лимит в 6 запросов/месяц навсегда)

**Решение:** Lunacy (Icons8) вместо Penpot — бесплатный десктоп-редактор (Win/Mac/Linux), нативный импорт `.fig`, и, что решающее, официальный MCP-сервер, который работает локально на компьютере Бори и создан с прицелом именно на связку с Claude. Работает, потому что Боря делает это на своей машине, где уже есть VS Code.

**Кто делает:** Боря — установка и перенос дизайна, Клод — доки и работа с MCP после подключения

1. Боря, установка (один раз):
   - [x] Установить Lunacy (icons8.com/lunacy) — бесплатно, без подписки
   - [x] Клонировать репозиторий `zadorozhnaya-portfolio` локально, открыть в VS Code — уже было готово, сессия и так работает локально на компьютере Бори
   - [x] Установить Claude Code (расширение VS Code или CLI через встроенный терминал) — уже стоял, версия 2.1.156
   - [x] Настроить свой git-доступ к GitHub локально — уже настроен (аккаунт titboan, пуши проходят)
2. Боря, перенос дизайна:
   - [x] Скопировать файл из Figma и открыть в Lunacy — простой способ через `Ctrl/Cmd+V` (Open URL From Clipboard) не сработал молча (вероятно версия Lunacy другая), вместо этого использован способ через `.fig`-файл: в Figma `File → Save local copy`, в Lunacy `File → Open...`. Импорт прошёл успешно
   - [x] Проверить именно эффект Rectangle56 (frosted glass, blur+opacity) — известное ограничение импорта: если блюров несколько, переносится только верхний. См. `ai-clone/feedback/figma-passthrough-blend-mode.md`. Боря подтвердил: выглядит адекватно, размытие не потерялось
   - [x] Включить в Lunacy MCP/LLM-интеграцию — на деле не «Settings → LLM Integration» как думали изначально, а отдельный пункт меню **AI → Model Context Protocol → MCP Server Enabled**, там же **Copy MCP Servers File**
3. Боря, подключение MCP к Claude Code:
   - [x] Добавить конфиг Lunacy в Claude Code — `claude mcp add-json` через PowerShell выдавал `Invalid input`, а `claude mcp add --scope user Lunacy -- "C:\Program Files\Lunacy\Lunacy.exe" -mcp` в самой PowerShell не сработал (PowerShell съедал разделитель `--`, команда падала на `unknown option '-mcp'`) — сработало через Git Bash той же машины с `claude mcp add --scope user Lunacy -- "C:\Program Files\Lunacy\Lunacy.exe" -mcp`. `claude mcp list` подтверждает: `Lunacy — ✓ Connected`. Зафиксировано в `ai-clone/feedback/claude-mcp-add-powershell-dashdash.md`
   - [x] Перезапустить Claude — рестарт произошёл, инструменты `mcp__Lunacy__*` появились в сессии. Проверено вызовом `get_viewport_info` — реально отвечает координатами вьюпорта открытого в Lunacy файла
4. Клод (в локальной сессии, после подключения):
   - [x] Проверить на реальной задаче — Боря выделил в Lunacy фрейм «солнечный берег», `get_selected` вернул точную структуру (`size: [1915, 25361]`, совпадает с Figma), `export scale=0.3 JPG` дал корректное превью. Подтверждено: Lunacy MCP пригоден для реальной работы, не только чтения метаданных
   - [x] Создать `.claude/skills/lunacy-workflow/SKILL.md` — новый основной навык, `figma-workflow` помечен как резервный
   - [x] Обновить раздел «Figma» в `CLAUDE.md` на «Figma / Lunacy» — Lunacy основной инструмент, Figma остаётся резервом
   - [x] Не удалять данные о старом Figma-файле — fileKey и структура секций оставлены в `CLAUDE.md`, `figma-workflow/SKILL.md` не удалён (нужен как резерв, пока не весь контент кейсов перенесён — при проверке 2026-07-20 обнаружено, что «Провыходной» и «монтажные области» из `plans/2026-06-18-figma-images-download.md` ещё не скачаны ни через Figma, ни через Lunacy)

**Важно:** эта часть плана работает только в локальной сессии Claude Code на компьютере Бори (через VS Code) — у текущей облачной сессии нет доступа к её экрану и локально запущенным приложениям вроде Lunacy.

**Запасной вариант:** если импорт `.fig` заметно потеряет эффекты — Penpot (облачный, тоже бесплатный, с бесплатной Inspect-панелью для ручного CSS-копипаста) остаётся резервным путём без необходимости что-то переустанавливать.

## Часть 5 — Привычка: использовать встроенные проверки

**Кто делает:** Клод

- [ ] После каждой секции — не просто «открыл в браузере», а `/run` (поднять + скриншот) и `/verify` (прогнать фичу end-to-end) вместо проверки на глаз
- [x] Playwright-пакет стоит (`npx playwright --version`), но браузер НЕ был установлен — уточнено 2026-07-21, пришлось ставить `npx playwright install chromium` (~300 МБ) отдельно. `localhost`/`127.0.0.1`/`::1` при этом недоступны из Bash/PowerShell — рабочий обход (`vite --host 0.0.0.0` + скролл по шагам перед скриншотом) записан в `CLAUDE.md`

---

## Порядок выполнения

1. Часть 1 (картинки) — сразу, не блокируется ничем внешним
2. Часть 2 (a11y) — сразу, следом за частью 1
3. Часть 3 (Vercel MCP) — как только Боря включит коннектор
4. Часть 4 (Lunacy) — по темпу Бори, требует установки на её компьютере (VS Code + Lunacy + Claude Code) — самая долгая часть
5. Часть 5 — не разовая задача, а правило на все последующие сессии

## Открытые вопросы

- Насколько хорошо `.fig`-импортёр Lunacy сохранит текущий файл — известное ограничение: несколько blur-слоёв схлопываются в один, компоненты становятся инстансами, star/polygon превращаются в обычные path. Прототипирование не переносится, но оно нам не нужно
- Если импорт заметно потеряет эффекты (особенно Rectangle56) — переходим на запасной вариант Penpot вместо пересоздания дизайна с нуля
