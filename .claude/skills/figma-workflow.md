# SKILL: Figma Workflow

## Когда использовать
При реализации любой новой секции из Figma или при вопросах про цвета/отступы/шрифты.

## Порядок работы

1. **Получить дизайн-контекст секции:**
   - Вызвать `get_design_context` с `fileKey: rA5AGt3zuM05YM7lzS0CoC` и нужным `nodeId`
   - NodeId основных секций:
     - Основной лендинг: `0:613`
     - Контент (портфолио): `0:44`
     - Services: `0:773`
     - Документы: `0:350`
     - Технология: `0:212`

2. **Извлечь:** цвета (HEX), шрифты (family, size, weight), отступы (padding, gap), border-radius

3. **Перенести в CSS-переменные** (`App.css`) если новый токен

4. **Скачать ассеты:** картинки → `public/images/`

## Figma файл
- fileKey: `rA5AGt3zuM05YM7lzS0CoC`
- Одна страница: Page 1 (0:1)
